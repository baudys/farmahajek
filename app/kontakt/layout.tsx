export const metadata = {
  title: 'Farma Hájek - Kontakt 📞',
}

const KontaktLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>
}

export default KontaktLayout
